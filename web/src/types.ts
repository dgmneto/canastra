/**
 * The engine's wire shapes.
 *
 * These now live in `@canastra/bots`, the leaf every consumer depends on. This
 * file is a re-export so the web's existing import paths keep working.
 */

export * from "@canastra/bots";
