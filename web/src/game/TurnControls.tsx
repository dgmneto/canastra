import type { GameClient } from "./client";
import type { PlayerView, Seat } from "@canastra/bots";

export function TurnControls(_props: {
  client: GameClient;
  view: PlayerView;
  seat: Seat;
}) {
  return null;
}
